--- a/device.c
+++ b/device.c
@@ -1768,6 +1768,8 @@ void cTSBuffer::Action(void)
                     break;
                     }
                  }
+              else
+                 cCondWait::SleepMs(10);
               }
            }
      }
